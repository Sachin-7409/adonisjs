@!component('../layouts/header', {title: 'create new user'})
  <div class="container-fluid">
    <section>
        <h1 class="display-1">
           Add new user
          </h1>  
    </section>
    <section>
        <div class="listUserTable">
            <form class="mt-5" method="POST" action="{{route('store')}}" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="exampleInputEmail1" class="form-label">Name</label>
                    <input type="text" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name="name">
                  @inputError('name')
                    @each(message in $messages)
                    <span class="text-danger">{{ message }}</span>
                    @end
                  @end
                  </div>
                <div class="mb-3">
                  <label for="exampleInputEmail1" class="form-label">Email address</label>
                  <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" name="email">
                  <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
                  @inputError('email')
                    @each(message in $messages)
                    <span class="text-danger">{{ message }}</span>
                    @end
                  @end
                </div>
                <div class="mb-3">
                  <label for="exampleInputPassword1" class="form-label">Password</label>
                  <input type="password" class="form-control" id="exampleInputPassword1" name="password">
                  @inputError('password')
                    @each(message in $messages)
                    <span class="text-danger">{{ message }}</span>
                    @end
                  @end
                </div>

                <div class="mb-3">
                  <label for="avatar" class="form-label">Img</label>
                  <input type="file" class="form-control" id="avatar" name="avatar">
                  
                </div>
                <div class="mb-3 form-check">
                  <input type="checkbox" class="form-check-input" id="exampleCheck1">
                  <label class="form-check-label" for="exampleCheck1">Check me out</label>
                </div>
                @!component('../components/button', { text: 'Submit',  className:'btn btn-primary'})
                @!component('../components/button', { text: 'Back', routeName: 'index' , className:'btn btn-danger', type: 'button'})
              </form>

        </div>
       
    </section>
  </div>
  
</body>
</html>